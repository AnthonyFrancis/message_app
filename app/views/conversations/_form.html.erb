<%= form_with(model: conversation) do |form| %>
  <% if conversation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(conversation.errors.count, "error") %> prohibited this conversation from being saved:</h2>

      <ul>
        <% conversation.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%#= form.submit %>

    <%= button_tag( :class => "bg-purple-700 p-3 rounded-full text-white text-sm flex flex-row space-x-3 gap-2 px-4") do %>
      <i class="far fa-paper-plane"></i>
      Send me a message
    <% end %>

  </div>
<% end %>
